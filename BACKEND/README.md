# Auth User Service Backend

## 소개

- TypeScript + Express 기반 백엔드 서버
- Layered 구조(route → controller → service → repository)
- Health Check API 포함
- 확장 가능한 구조 설계

## 실행

```bash
npm install
npm run dev
```
